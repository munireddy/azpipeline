trigger:
- master

pool:
  vmImage: 'ubuntu-latest'
steps:
  # Sample command to test echo
  - script: echo This is a build pipeline using Azure-pipelines
  # This is a sample command to test mkdir
  - script: mkdir sampledir

  # Actual script needed for you.
  - script: |
        ls -ltr 
        az login --service-principal --username "3d587273-dcac-47f1-b714-72f244c67c52"  --password "8ec3a20c-e55e-4341-abec-28d57eab3be1" --tenant "59b2865a-7fb8-4ccb-ab68-72cbca88fc48"
        # Update for your admin password
        AdminPassword=MLK123ATL!!ffD$
        az group create --name myResourceGroup2 --location westeurope
        az network vnet create --resource-group myResourceGroup2 --name myVnet2 --subnet-name mySubnet2
        az network public-ip create --resource-group myResourceGroup2 --name myPublicIP2

        az network nsg create --resource-group myResourceGroup2 --name myNetworkSecurityGroup2

        az network nic create \
           --resource-group myResourceGroup2 \
           --name myNic2 \
           --vnet-name myVnet2 \
           --subnet mySubnet2 \
           --network-security-group myNetworkSecurityGroup2 \
           --public-ip-address myPublicIP2


        #  az vm create \
        #   --resource-group myResourceGroup \
        #   --name myVM \
        #   --location westeurope \
        #   --nics myNic \
        #   --image win2016datacenter \
        #   --admin-username azureuser \
        #   --admin-password $AdminPassword
        az vm create \
           --resource-group myResourceGroup2 \
           --name myVM2 \
           --image CentOS \
           --admin-username azureuser \
           --admin-password $AdminPassword \
           --custom-data cloud-init.txt \
           --generate-ssh-keys    

        # az vm open-port --port 3389 --resource-group myResourceGroup --name myVM
