trigger:
- master

pool:
  vmImage: 'ubuntu-latest'
steps:
  # Sample command to test echo
  - script: echo This is a build pipeline using Azure-pipelines
  # This is a sample command to test mkdir
  - script: mkdir sampledir

  # Actual script needed for you.
  - script: |
        ls -ltr 
        az login --service-principal --username "3d587273-dcac-47f1-b714-72f244c67c52"  --password "8ec3a20c-e55e-4341-abec-28d57eab3be1" --tenant "59b2865a-7fb8-4ccb-ab68-72cbca88fc48"
        # Update for your admin password
        AdminPassword=MLK123ATL!!ffD$
        az group create --name myResourceGroup5u --location westeurope
        az network vnet create --resource-group myResourceGroup5 --name myVnet5 --subnet-name mySubnet5
        az network public-ip create --resource-group myResourceGroup5 --name myPublicIP5

        az network nsg create --resource-group myResourceGroup5 --name myNetworkSecurityGroup5

        az network nic create \
           --resource-group myResourceGroup5 \
           --name myNic5 \
           --vnet-name myVnet5 \
           --subnet mySubnet5 \
           --network-security-group myNetworkSecurityGroup5 \
           --public-ip-address myPublicIP5


        #  az vm create \
        #   --resource-group myResourceGroup \
        #   --name myVM \
        #   --location westeurope \
        #   --nics myNic \
        #   --image win2016datacenter \
        #   --admin-username azureuser \
        #   --admin-password $AdminPassword
        az vm create \
           --resource-group myResourceGroup5 \
           --name myVM5 \
           --location westeurope \
           --image CentOS \
           --admin-username azureuser \
           --admin-password $AdminPassword \
           --generate-ssh-keys
           
        az vm run-command invoke -g myResourceGroup5 -n myVm5 --command-id RunShellScript --scripts "sudo yum update && sudo touch /tmp/file1.txt && curl -fsSL https://get.docker.com/ | sh"
        # az vm open-port --port 3389 --resource-group myResourceGroup --name myVM
